# Search a 2D Matrix

## 解法：二分查找 [BinarySearch.cpp](BinarySearch.cpp)

### 算法思路

矩阵满足两个条件：
- 每行从左到右递增
- 每行的首元素大于上一行的末元素

这意味着矩阵本质上是一个**展平的有序数组**。将虚拟下标 `mid` 映射到二维坐标：

```
row = mid / n
col = mid % n
```

对 `[0, m*n-1]` 做标准二分即可。

### 复杂度分析

- **时间复杂度：O(log(m×n))**
- **空间复杂度：O(1)**
